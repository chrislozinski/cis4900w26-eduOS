# live-config.service ships with no start timeout ("no limit"), so a wedged
# component hangs boot forever with no logs reachable. Cap it: a stuck
# live-config fails visibly after 2 minutes and boot continues.
[Service]
TimeoutStartSec=120
