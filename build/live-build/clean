#!/usr/bin/env bash
set -euo pipefail
cd "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# noauto prevents lb clean from calling this script again (avoids infinite loop).
lb clean noauto --purge || true
