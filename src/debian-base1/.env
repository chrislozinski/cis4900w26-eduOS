# Teacher machine LAN IP for Docker student testing. Edit once; docker compose
# reads this file automatically, so plain "docker compose up --build" works.
# Leave TEACHER_IP empty when this container IS the teacher.
TEACHER_IP=192.168.68.140
TEACHER_TCP_PORT=49002
